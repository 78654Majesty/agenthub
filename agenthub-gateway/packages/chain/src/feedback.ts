/**
 * @file feedback.ts
 * @owner 基建
 * @module chain
 *
 * Feedback submission to on-chain agent records.
 *
 * Exports:
 *   - submitReceiptFeedback(agentAsset, receipt, sdk)
 *   - submitRatingFeedback(agentAsset, rating, sdk)
 */
