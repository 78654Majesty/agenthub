/**
 * @file agent-register.ts
 * @owner 基建
 * @module chain
 *
 * Agent on-chain registration via ERC-8004.
 *
 * Exports:
 *   - registerAgentOnChain(agent, sdk)
 *   - buildRegistrationFile(agent)
 */
