/**
 * @file ipfs-client.ts
 * @owner 基建
 * @module chain
 *
 * IPFS operations for uploading and fetching data.
 *
 * Exports:
 *   - uploadJson(data)
 *   - fetchJson(cid)
 *   - uploadFile(buffer)
 */
