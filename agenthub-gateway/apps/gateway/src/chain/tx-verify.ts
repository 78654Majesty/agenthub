/**
 * @file  src/chain/tx-verify.ts
 * @owner 基建
 * @module chain
 *
 * x402 transaction verification via Solana getTransaction.
 * Confirms on-chain payment transactions match expected parameters.
 *
 * Type hints:
 *   - ../config (config)
 *   - ./erc8004 (sdk)
 *
 * Exports:
 *   - verifyTransaction(txSignature, expected)
 */
